curl_cffi>=0.6.0
python-dotenv>=1.0.0
flask>=3.0.0
gunicorn>=22.0.0
tzdata>=2024.1
cryptography>=42.0.0
bcrypt>=4.0.0
# APNs（Phase 3）: ES256 JWT 签名 + HTTP/2 客户端
pyjwt[crypto]>=2.8.0
httpx>=0.27.0
h2>=4.0.0
# reCAPTCHA solving (Xior / OurDomain auto-booking)
2captcha-python>=2.0.0
# CloakBrowser: stealth Chromium for H2S Cloudflare bypass
# 下限锁在 0.5.x：0.4/0.5 在 0.x 语义下允许破坏性变更，只有 >=0.5.3 实测验证过。
cloakbrowser>=0.5.3
