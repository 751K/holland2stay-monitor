# ══════════════════════════════════════════════════════════════════════
# requirements.lock — 精确版本锁（用于 Docker 构建，保证可重复性）
#
# 生成方式（在 Docker 构建环境内运行）：
#   docker run --rm python:3.11-slim-bookworm \
#     sh -c "pip install -r requirements.txt && pip freeze" \
#     > requirements.lock
#
# 日常开发（本地）仍用 requirements.txt：
#   pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════════

# ── 直接依赖 ─────────────────────────────────────────────────────────
certifi==2025.1.31
cffi==1.17.1
cryptography==43.0.3
curl_cffi==0.14.0
Flask==3.1.3
gunicorn==26.0.0
python-dotenv==1.2.2
tzdata==2026.2

# ── App 登录 ────────────────────────────────────────────────────────────
bcrypt==5.0.0

# ── APNs (Phase 3) — ES256 JWT + HTTP/2 ───────────────────────────────
PyJWT==2.12.1
httpx==0.28.1
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0
httpcore==1.0.9
anyio==4.12.1
sniffio==1.3.1
idna==3.7

# ── Flask 传递依赖 ────────────────────────────────────────────────────
blinker==1.9.0
click==8.3.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8

# ── gunicorn 传递依赖 ─────────────────────────────────────────────────
packaging==24.2

# ── cffi 传递依赖（cryptography / curl_cffi 共享）────────────────────
pycparser==2.21
